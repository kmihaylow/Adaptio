# --- Optional: only needed for AI plan adaptation (weekly coach review) ---
# console.anthropic.com -> API keys
ANTHROPIC_API_KEY=

# --- Optional: intervals.icu integration (Garmin data flows through it) ---
# intervals.icu -> Settings -> Developer -> generate API key
INTERVALS_API_KEY=
# your athlete id, e.g. i123456 (visible in the intervals.icu URL)
INTERVALS_ATHLETE_ID=

# --- Server ---
ADAPTIO_DB_PATH=adaptio.db
